version: '3'

tasks:
  launch-accounting-fresh:
    internal: true
    desc: Fail closed until the isolated fresh accounting launcher exists.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and no fresh scenario is claimed yet.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs accounting-fresh

  launch-simulator:
    internal: true
    desc: Fail closed until the synthetic network-disabled simulator owner exists.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and no simulator readiness is claimed yet.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs simulator

  launch-evte:
    internal: true
    desc: Fail closed until the non-production EVTE scenario owner exists.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and no EVTE readiness is claimed yet.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs evte

  run-doctor:
    internal: true
    desc: Fail closed until the authenticated SBR readiness doctor exists.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and no doctor readiness is claimed yet.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs doctor

  run-registration-check:
    internal: true
    desc: Fail closed until the SBR registration readiness owner exists.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and registration readiness is not claimed.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs registration

  run-test:
    internal: true
    desc: Fail closed until the focused SBR test owners exist.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and no SBR verification is claimed yet.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs test

  run-evidence:
    internal: true
    desc: Fail closed until the SBR evidence owner exists.
    summary: |
      The simulator is synthetic and network-disabled. EVTE is non-production and requires signed external inputs.
      No live credential is accepted by Task. BAS remains preparation-only with no submit or lodge action.
      Production SBR is unavailable, and no evidence bundle is created yet.
    preconditions:
      - sh: >-
          mise exec -- node -e 'const target = `${process.platform}/${process.arch}`; if (target !== "darwin/arm64") { console.error(`UNSUPPORTED_SBR_TARGET:${target}`); process.exit(1); }'
        msg: UNSUPPORTED_SBR_TARGET:{{OS}}/{{ARCH}}
    cmds:
      - mise exec -- node scripts/sbr-incomplete.mjs evidence
